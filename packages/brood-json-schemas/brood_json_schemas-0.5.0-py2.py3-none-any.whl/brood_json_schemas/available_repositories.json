{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Available repositories",
    "description": "Repository names to check for availability.",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "repositories": {
            "description": "Repository names to check for availability.",
            "type": "array",
            "items": {
                "type": "string"
            }
        }
    },
    "required": ["repositories"]
}
