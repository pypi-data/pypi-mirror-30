from distutils.core import setup
setup(
    name = 'pykov',
    packages = ['pykov'], # this must be the same as the name above
    version = '0.1',
    description = 'Simple Markov chain manipulation',
    author = 'silicWulf',
    author_email = 'me@siliconwolf.pw',
    url = 'https://github.com/silicWulf/pykov', # use the URL to the github repo
    download_url = 'https://github.com/peterldowns/mypackage/archive/0.1.tar.gz', # I'll explain this in a second
    keywords = ['markov', 'chain', 'simple'], # arbitrary keywords
    classifiers = [],
)