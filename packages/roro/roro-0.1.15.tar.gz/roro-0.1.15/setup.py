#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['roro']

package_data = \
{'': ['*']}

install_requires = \
['firefly-python>=0.1.7',
 'click>=6.7',
 'tabulate>=0.7.7',
 'PyYAML>=3.12',
 'joblib>=0.11']

extras_require = \
{":python_version < '3'": ['pathlib>=1.0.1', 'backports.tempfile']}

entry_points = \
{'console_scripts': ['roro = roro.cli:cli', 'roro-dev = roro.cli:main_dev']}

setup(name='roro',
      version='0.1.15',
      description='roro - commandline tool for accessing all the sevices in RorodataPlatform.',
      author='rorodata',
      author_email='rorodata.team@gmail.com',
      url='https://github.com/rorodata/roro-client',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
     )
