from setuptools import find_packages, setup

packages = find_packages()

setup(
    name = 'ThreadDownloader8527',
    version = '1.0.1',
    description = "my first python distribution",
    packages = packages
)